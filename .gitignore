node_modules/
*.log

# Real configs (templates are checked in as *.example.*)
track-server/config/config.json
track-server/config/google-oauth-secret.json
track-server/config/sheets.googleapis.com-nodejs-quickstart.json
cloud-api/config.js

# Runtime data — these dirs hold user uploads on the live host, not source
cloud-api/cars/
cloud-api/checkin/
cloud-api/videos/
cloud-api/tmp/
track-server/public/cars/
track-server/videos/

# Local working area; not part of the repo
_inventory/

# OS / editor noise
.DS_Store
.vscode/
*.swp
